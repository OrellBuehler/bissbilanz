.svelte-kit
build
node_modules
drizzle
src/lib/paraglide
src/paraglide

# inlang generates these at compile time (mirror project.inlang/.gitignore — only settings.json is tracked)
project.inlang/*
!project.inlang/settings.json

# Generated by `bun run analytics:generate` and guarded by `analytics:check` —
# prettier's array wrapping will never match a generator's, and letting it
# reformat the file makes the drift check fail forever.
analytics-parity/fixtures/golden-vectors.json
