<?xml version="1.0" encoding="utf-8"?>
<!-- No previewLayout: the host then falls back to previewImage, which is honest
     about the widget rather than showing another widget's skeleton in the picker. -->
<appwidget-provider xmlns:android="http://schemas.android.com/apk/res/android"
    android:minWidth="250dp"
    android:minHeight="180dp"
    android:minResizeWidth="250dp"
    android:minResizeHeight="150dp"
    android:targetCellWidth="4"
    android:targetCellHeight="4"
    android:resizeMode="horizontal|vertical"
    android:updatePeriodMillis="0"
    android:description="@string/day_overview_widget_description"
    android:previewImage="@mipmap/ic_launcher"
    android:initialLayout="@layout/favorites_widget_preview"
    android:widgetCategory="home_screen" />
