<?xml version="1.0" encoding="utf-8"?>
<!-- The locales the app actually ships translations for. Drives the system's
     per-app language screen on API 33+ and bounds the in-app picker. -->
<locale-config xmlns:android="http://schemas.android.com/apk/res/android">
    <locale android:name="en" />
    <locale android:name="de" />
</locale-config>
