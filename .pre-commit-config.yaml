repos:
  - repo: local
    hooks:
      - id: prettier
        name: prettier
        entry: bunx prettier --write --ignore-unknown
        language: system
        types_or:
          - javascript
          - ts
          - svelte
          - css
          - json
          - yaml
          - markdown
          - html

      - id: svelte-check
        name: svelte-check
        entry: bash -c 'bun run paraglide:compile && bunx svelte-kit sync && bunx svelte-check --tsconfig ./tsconfig.json'
        language: system
        pass_filenames: false
        types_or:
          - ts
          - svelte

      - id: ktlint
        name: ktlint
        entry: ktlint
        language: system
        types:
          - kotlin

      - id: swiftformat
        name: swiftformat
        entry: bash -c 'command -v swiftformat >/dev/null 2>&1 && swiftformat --lint --config mobile/iosApp/.swiftformat || echo "swiftformat not installed, skipping"'
        language: system
        types:
          - swift

      - id: gitleaks
        name: gitleaks
        entry: gitleaks protect --staged --verbose --redact
        language: system
        pass_filenames: false
