# PWA & Offline Support — Progress

## Current Status

**Batch 2 complete (Tasks 4-7).** Ready to commit on `features` branch.

## Completed Tasks

- [x] **Task 1: Install Prompt Banner** — `InstallBanner.svelte` created, added to app layout above sidebar
- [x] **Task 2: Offline Indicator** — `OfflineIndicator.svelte` created, added below header in layout
- [x] **Task 3: Offline Write Queue (IndexedDB)** — `offline-queue.ts` created with enqueue/drain/remove
- [x] **Task 4: Offline-Aware Fetch Wrapper** — `src/lib/utils/api.ts` with `apiFetch()` that queues writes when offline
- [x] **Task 5: Background Sync on Reconnect** — `src/lib/stores/sync.ts` with `syncQueue()` and `startSyncListener()`
- [x] **Task 6: Migrate Dashboard to apiFetch** — Dashboard write operations now use `apiFetch()`
- [x] **Task 7: Auto-Update Toast** — `UpdateToast.svelte` detects new SW and shows refresh prompt
- [x] **Task 8: Verify Full PWA Setup** — `bun run check` passes (0 new errors; pre-existing servingUnit type issues only)

## Files Created/Modified

| File | Action |
|------|--------|
| `src/lib/components/pwa/InstallBanner.svelte` | Created (Batch 1) |
| `src/lib/components/pwa/OfflineIndicator.svelte` | Created (Batch 1) |
| `src/lib/stores/offline-queue.ts` | Created (Batch 1) |
| `src/lib/utils/api.ts` | Created — offline-aware fetch wrapper |
| `src/lib/stores/sync.ts` | Created — background sync on reconnect |
| `src/lib/components/pwa/UpdateToast.svelte` | Created — SW update toast |
| `src/routes/app/+layout.svelte` | Modified — imports sync, UpdateToast |
| `src/routes/app/+page.svelte` | Modified — write calls use apiFetch |
| `messages/en.json` | Modified (Batch 1) — `pwa_*` keys |
| `messages/de.json` | Modified (Batch 1) — `pwa_*` keys |

## Notes & Learnings

- All pre-existing type errors are `servingUnit` mismatches in `foods.ts` and test files — unrelated to PWA work
- Icon imports use individual paths: `from '@lucide/svelte/icons/x'`
- All user-facing strings use Paraglide i18n
- Paraglide messages must be regenerated by running `bun run dev` briefly before `bun run check`

## WHERE TO CONTINUE

**All tasks complete.** The PWA offline support implementation is finished. Ready to:
1. Commit all changes
2. Move plan to `docs/finished/` and delete progress file
