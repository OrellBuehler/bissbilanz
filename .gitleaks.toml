[extend]
useDefault = true

[allowlist]
description = "Allowlist Open Food Facts field names and i18n keys — these match generic-api-key entropy heuristic but are not secrets"
regexes = [
  '''[\w-]+_100g''',
  '''nutrient_\w+''',
]
