---
phase: 02-favorites
plan: 02
type: execute
wave: 2
depends_on: ["02-01"]
files_modified:
  - src/lib/components/favorites/FavoriteCard.svelte
  - src/lib/components/favorites/FavoritesGrid.svelte
  - src/lib/components/favorites/ServingsPicker.svelte
  - src/routes/app/favorites/+page.svelte
autonomous: true
requirements:
  - FAV-03
  - FAV-05
  - FAV-08
  - FAV-09

must_haves:
  truths:
    - "Favorites page shows favorite foods and recipes as image-first cards in a 2-column grid"
    - "Cards display name, image (or colored placeholder with first letter), and all four macros (cal, protein, carbs, fat)"
    - "Foods and recipes are separated by tabs on the favorites page"
    - "Tapping a card logs 1 serving to the current meal (time-based) and shows a toast with Undo"
    - "Undo deletes the entry and refreshes the data"
    - "Empty state shows a friendly message with a 'Browse foods' CTA linking to /app/foods"
  artifacts:
    - path: "src/lib/components/favorites/FavoriteCard.svelte"
      provides: "Reusable image-first card with macro info and tap handler"
      min_lines: 40
    - path: "src/lib/components/favorites/FavoritesGrid.svelte"
      provides: "2-column responsive grid wrapper for FavoriteCard"
      min_lines: 15
    - path: "src/lib/components/favorites/ServingsPicker.svelte"
      provides: "Dialog for choosing servings before logging"
      min_lines: 30
    - path: "src/routes/app/favorites/+page.svelte"
      provides: "Favorites page with tabs, grid, and tap-to-log"
      min_lines: 80
  key_links:
    - from: "src/routes/app/favorites/+page.svelte"
      to: "/api/favorites"
      via: "fetch on mount"
      pattern: "fetch.*api/favorites"
    - from: "src/routes/app/favorites/+page.svelte"
      to: "/api/entries"
      via: "POST on tap-to-log"
      pattern: "fetch.*api/entries.*POST"
    - from: "src/lib/components/favorites/FavoriteCard.svelte"
      to: "src/routes/app/favorites/+page.svelte"
      via: "onTap prop callback"
      pattern: "onTap"
---

<objective>
Build the FavoriteCard component and the favorites page with tabbed layout, tap-to-log, and undo toast.

Purpose: This is the core user-facing feature — visual favorite cards that users tap to instantly log food.
Output: FavoriteCard.svelte (reusable), FavoritesGrid.svelte, ServingsPicker.svelte, /app/favorites page.
</objective>

<execution_context>
@/home/orell/.claude/get-shit-done/workflows/execute-plan.md
@/home/orell/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/02-favorites/02-RESEARCH.md
@.planning/phases/02-favorites/02-CONTEXT.md
@.planning/phases/02-favorites/02-01-SUMMARY.md
@src/lib/components/entries/AddFoodModal.svelte
@src/routes/app/+page.svelte
@src/routes/app/+layout.svelte
@src/lib/utils/meals.ts
</context>

<tasks>

<task type="auto">
  <name>Task 1: FavoriteCard and FavoritesGrid components</name>
  <files>
    src/lib/components/favorites/FavoriteCard.svelte
    src/lib/components/favorites/FavoritesGrid.svelte
  </files>
  <action>
    1. Create `src/lib/components/favorites/FavoriteCard.svelte`:

    **Props (Svelte 5 runes):**
    ```typescript
    type Props = {
      name: string;
      imageUrl?: string | null;
      calories: number;
      protein: number;
      carbs: number;
      fat: number;
      type: 'food' | 'recipe';
      onTap: () => void;
    };
    ```

    **Layout (per locked decisions):**
    - Image-first: large image area at top (aspect-ratio 4:3), compact info below
    - Entire card is a `<button>` with `onclick={onTap}` — no separate tap target
    - No favorite toggle (heart/star) on the card — per locked decision

    **Image area:**
    - If `imageUrl` exists: `<img>` with `object-cover`, `loading="lazy"`
    - If no image: colored placeholder with food's first letter (uppercase), like contact avatars
    - Placeholder colors: deterministic from name hash (reduce charCodes % palette length)
    - Color palette: 8 distinct pastel colors (rose, sky, amber, emerald, violet, orange, teal, pink — use Tailwind `bg-*-200` for background, `text-*-700` for the letter)

    **Info area (below image):**
    - Food name: truncated with `truncate`, `text-sm font-medium`
    - Macro row: all four macros inline, using the project color coding:
      - Calories = blue (`text-blue-600`), format: `{Math.round(calories)} kcal`
      - Protein = red (`text-red-600`), format: `{protein.toFixed(1)}g P`
      - Carbs = orange (`text-orange-600`), format: `{carbs.toFixed(1)}g C`
      - Fat = yellow (`text-yellow-600`), format: `{fat.toFixed(1)}g F`
    - Use `text-xs text-muted-foreground` base, with colored spans

    **Card styling:**
    - `overflow-hidden rounded-xl border bg-card shadow-sm`
    - Subtle hover: `hover:shadow-md transition-shadow`
    - Active press state: `active:scale-[0.98] transition-transform`

    2. Create `src/lib/components/favorites/FavoritesGrid.svelte`:

    **Props:**
    ```typescript
    type Props = {
      children: Snippet;
    };
    ```

    **Layout:**
    - CSS grid: `grid grid-cols-2 gap-3 sm:grid-cols-3 lg:grid-cols-4`
    - Uses `{@render children()}` to wrap card children
    - This is a simple layout wrapper to ensure consistent grid spacing everywhere FavoriteCards are used
  </action>
  <verify>`bun run check` passes with no type errors on the new components</verify>
  <done>FavoriteCard renders image-first cards with colored placeholders and macro info; FavoritesGrid provides consistent responsive grid layout</done>
</task>

<task type="auto">
  <name>Task 2: Favorites page with tabs, tap-to-log, and undo</name>
  <files>
    src/lib/components/favorites/ServingsPicker.svelte
    src/routes/app/favorites/+page.svelte
  </files>
  <action>
    1. Create `src/lib/components/favorites/ServingsPicker.svelte`:
    - A Dialog that lets the user pick servings before logging
    - Props: `open: boolean`, `itemName: string`, `onConfirm: (servings: number) => void`, `onClose: () => void`
    - Contains a numeric input (default 1, min 0.25, step 0.25) and a "Log" button
    - Used when user preference `favoriteTapAction` is `'picker'`
    - Use shadcn-svelte Dialog, Input, Button, Label components

    2. Create `src/routes/app/favorites/+page.svelte`:

    **Data loading (onMount):**
    - Fetch GET `/api/favorites` to get `{ foods, recipes }`
    - Also fetch GET `/api/preferences` to read `favoriteTapAction` preference
    - Store in reactive state

    **Tab layout (per locked decisions):**
    - Use shadcn-svelte Tabs component
    - Two tabs: "Foods" and "Recipes"
    - Each tab renders a FavoritesGrid with FavoriteCards

    **Tap-to-log flow (Claude's discretion — implementing as follows):**
    - Import `getCurrentMealByTime` from `$lib/utils/meals`
    - Import `today` from `$lib/utils/dates`
    - If `favoriteTapAction === 'instant'` (default):
      - On card tap: immediately POST to `/api/entries` with `{ foodId/recipeId, mealType: getCurrentMealByTime(), servings: 1, date: today() }`
      - Show success toast via `svelte-sonner`: `toast.success('Logged {name} to {meal}', { action: { label: 'Undo', onClick: undoFn }, duration: 5000 })`
    - If `favoriteTapAction === 'picker'`:
      - On card tap: open ServingsPicker dialog
      - On confirm: POST to `/api/entries` with chosen servings, then show toast with undo

    **Undo flow:**
    - The undo callback: `DELETE /api/entries/${entryId}`, then refresh favorites data
    - Entry ID comes from the POST response (`{ entry: { id } }`)

    **Empty state (per locked decisions):**
    - When foods/recipes arrays are empty for the active tab
    - Friendly message: "No favorites yet" with illustration placeholder (use a large muted icon from lucide-svelte, e.g., `Heart` or `Star`)
    - CTA button: "Browse foods" linking to `/app/foods`

    **Loading state:**
    - Show skeleton cards or spinner while data loads
    - Use a simple `loading` boolean with conditional rendering

    **Add to the navigation:** Update the app layout's nav to include a link to `/app/favorites` if it doesn't already exist. Check `src/routes/app/+layout.svelte` for the nav structure.

    **i18n:** Add Paraglide message keys for all user-facing strings (favorites_title, favorites_foods_tab, favorites_recipes_tab, favorites_empty, favorites_browse_foods, favorites_logged_toast, favorites_undo). Create the messages in `messages/en.json` and `messages/de.json`.
  </action>
  <verify>
    - `bun run check` passes
    - Navigate to `/app/favorites` — page renders with tabs
    - If favorites exist, cards display in 2-column grid with images/placeholders and macros
    - Tapping a card logs an entry and shows toast with undo
  </verify>
  <done>Favorites page shows food and recipe favorites as image cards in tabs, tap-to-log works with undo toast, empty state shows Browse Foods CTA</done>
</task>

</tasks>

<verification>
- `bun run check` passes with no type errors
- Favorites page accessible at /app/favorites
- FavoriteCard displays image (or placeholder) + all four macros
- 2-column grid on mobile, 3-4 on wider screens
- Tab switching between Foods and Recipes works
- Tap-to-log creates entry and shows toast with undo
- Undo deletes the entry
- Empty state displays when no favorites exist
</verification>

<success_criteria>
- FavoriteCard is a single reusable component with image-first layout, colored placeholders, and macro display
- Favorites page has tabbed layout separating foods and recipes
- Tap-to-log flow works end-to-end: tap card -> log entry -> toast with undo -> undo deletes entry
- All new components pass type checking
</success_criteria>

<output>
After completion, create `.planning/phases/02-favorites/02-02-SUMMARY.md`
</output>
