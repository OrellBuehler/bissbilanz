---
phase: 02-favorites
plan: 03
type: execute
wave: 3
depends_on: ["02-02"]
files_modified:
  - src/lib/components/favorites/FavoritesWidget.svelte
  - src/routes/app/+page.svelte
  - src/lib/components/entries/AddFoodModal.svelte
  - src/routes/app/foods/[id]/+page.svelte
  - src/routes/app/foods/[id]/+page.server.ts
  - src/routes/app/recipes/[id]/+page.svelte
  - src/routes/app/recipes/[id]/+page.server.ts
  - src/lib/server/validation/recipes.ts
  - src/lib/server/recipes.ts
autonomous: true
requirements:
  - FAV-06
  - FAV-01
  - FAV-02

must_haves:
  truths:
    - "Dashboard shows a favorites widget with top 5 favorites using the same FavoriteCard style"
    - "Dashboard widget is only visible when showFavoritesWidget preference is enabled"
    - "Tapping a card in the dashboard widget logs to current meal with undo toast (same as favorites page)"
    - "AddFoodModal favorites tab shows FavoriteCard grid instead of the old text list"
    - "User can navigate to a food detail page, toggle isFavorite, and upload an image"
    - "User can navigate to a recipe detail page, toggle isFavorite, and upload an image"
    - "Recipe PATCH endpoint accepts and persists isFavorite and imageUrl fields"
    - "Start page redirect points to /app/favorites instead of /app/foods"
  artifacts:
    - path: "src/lib/components/favorites/FavoritesWidget.svelte"
      provides: "Dashboard widget showing top 5 favorites with tap-to-log"
      min_lines: 40
    - path: "src/routes/app/foods/[id]/+page.svelte"
      provides: "Food detail/edit page with image upload and favorite toggle"
      min_lines: 60
    - path: "src/routes/app/recipes/[id]/+page.svelte"
      provides: "Recipe detail/edit page with image upload and favorite toggle"
      min_lines: 60
    - path: "src/lib/server/validation/recipes.ts"
      provides: "Extended recipe validation schema with isFavorite and imageUrl"
      contains: "isFavorite"
  key_links:
    - from: "src/lib/components/favorites/FavoritesWidget.svelte"
      to: "/api/favorites"
      via: "fetch with limit=5, client-side merge and sort"
      pattern: "api/favorites.*limit=5"
    - from: "src/lib/components/favorites/FavoritesWidget.svelte"
      to: "src/lib/components/favorites/FavoriteCard.svelte"
      via: "import FavoriteCard"
      pattern: "FavoriteCard"
    - from: "src/routes/app/+page.svelte"
      to: "src/lib/components/favorites/FavoritesWidget.svelte"
      via: "conditional render based on showFavoritesWidget"
      pattern: "showFavoritesWidget"
    - from: "src/lib/server/validation/recipes.ts"
      to: "src/lib/server/recipes.ts"
      via: "recipeUpdateSchema imported for PATCH validation"
      pattern: "recipeUpdateSchema"
---

<objective>
Wire favorites into the dashboard (widget), AddFoodModal (card grid), and create food/recipe detail pages with image upload and favorite toggle. Extend recipe validation schema. Fix start page redirect.

Purpose: Completes the favorites integration across all touchpoints in the app.
Output: FavoritesWidget on dashboard, upgraded AddFoodModal, new food/recipe detail pages, extended recipe schema, start page fix.
</objective>

<execution_context>
@/home/orell/.claude/get-shit-done/workflows/execute-plan.md
@/home/orell/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/02-favorites/02-RESEARCH.md
@.planning/phases/02-favorites/02-CONTEXT.md
@.planning/phases/02-favorites/02-01-SUMMARY.md
@.planning/phases/02-favorites/02-02-SUMMARY.md
@src/routes/app/+page.svelte
@src/lib/components/entries/AddFoodModal.svelte
@src/lib/components/favorites/FavoriteCard.svelte
@src/lib/components/favorites/FavoritesGrid.svelte
@src/lib/server/validation/recipes.ts
@src/lib/server/recipes.ts
@src/routes/app/foods/+page.svelte
@src/routes/app/recipes/+page.svelte
</context>

<tasks>

<task type="auto">
  <name>Task 1: Dashboard favorites widget with combined ranking</name>
  <files>
    src/lib/components/favorites/FavoritesWidget.svelte
    src/routes/app/+page.svelte
  </files>
  <action>
    1. Create `src/lib/components/favorites/FavoritesWidget.svelte`:

    **Behavior:**
    - On mount, fetch `GET /api/favorites?limit=5` to get favorites (API returns `{ foods: [...], recipes: [...] }`)
    - **Client-side merge:** Concatenate both arrays, sort by `logCount` descending, slice to 5. This produces a single combined "top 5" list ranked across both foods and recipes.
    - If no favorites returned after merge, don't render anything (widget disappears gracefully)
    - Render a Card.Root wrapper with header "Favorites" and a FavoritesGrid of FavoriteCards inside

    **Tap-to-log:**
    - Reuse the same tap-to-log pattern as the favorites page:
      - Import `getCurrentMealByTime` and `today`
      - On tap: POST to `/api/entries`, show toast with undo via svelte-sonner
      - The widget should accept an `onEntryLogged` callback prop so the dashboard can refresh its entry data after logging

    **Per locked decision:** Uses the same image-first card style as the favorites page (not a compact/mini variant). Same FavoriteCard component.

    2. Update `src/routes/app/+page.svelte` (the dashboard):

    **Add the widget:**
    - Import FavoritesWidget
    - Load user preferences on mount (already fetched in `checkStartPage` — extract `showFavoritesWidget` from the response)
    - Render `<FavoritesWidget onEntryLogged={loadData} />` conditionally when `showFavoritesWidget` is true
    - Place it between the weekly chart and the supplement checklist in the layout

    **Fix start page redirect:**
    - In `checkStartPage()`, change `goto('/app/foods', ...)` to `goto('/app/favorites', { replaceState: true })`
    - Remove the TODO comment about Phase 2
  </action>
  <verify>
    - `bun run check` passes
    - Dashboard shows favorites widget when preference is enabled and favorites exist
    - Widget shows up to 5 cards (combined from foods and recipes, sorted by logCount)
    - Tapping a card in widget logs entry, shows toast, and dashboard refreshes
    - Start page redirect goes to /app/favorites
  </verify>
  <done>Dashboard favorites widget shows top 5 favorites (combined ranking across foods and recipes) with tap-to-log, respects showFavoritesWidget preference, and start page redirect now points to /app/favorites</done>
</task>

<task type="auto">
  <name>Task 2: Extend recipe validation schema and create food/recipe detail pages</name>
  <files>
    src/lib/server/validation/recipes.ts
    src/routes/app/foods/[id]/+page.svelte
    src/routes/app/foods/[id]/+page.server.ts
    src/routes/app/recipes/[id]/+page.svelte
    src/routes/app/recipes/[id]/+page.server.ts
  </files>
  <action>
    1. **Extend recipe validation schema** in `src/lib/server/validation/recipes.ts`:
    - Add `isFavorite: z.boolean().optional()` to `recipeCreateSchema`
    - Add `imageUrl: z.string().optional().nullable()` to `recipeCreateSchema`
    - Since `recipeUpdateSchema = recipeCreateSchema.partial()`, these will automatically be available for PATCH
    - This ensures PATCH `/api/recipes/:id` accepts and persists `isFavorite` and `imageUrl` instead of silently dropping them

    2. **Create food detail/edit page** at `src/routes/app/foods/[id]/+page.svelte` (NEW FILE — does not exist):

    **Page server** (`+page.server.ts`):
    - Load food by ID from the URL param via `GET /api/foods/:id` or direct Drizzle query
    - Verify the food belongs to the current user (401/404 otherwise)

    **Page component:**
    - Display a form pre-populated with the food's current data (name, serving size, calories, protein, carbs, fat, fiber)
    - **Image upload field** (per locked decision: native file input, no client-side crop):
      - `<input type="file" accept="image/*">` with label
      - On file selection: POST to `/api/images/upload` with FormData, get back `{ imageUrl }`
      - Update the food's `imageUrl` via PATCH `/api/foods/:id`
      - Show current image preview if imageUrl exists (use `<img>` with the imageUrl)
      - Show colored placeholder (same style as FavoriteCard) if no image
    - **Favorite toggle:**
      - shadcn-svelte Switch or Checkbox labeled "Mark as favorite"
      - Bound to the food's `isFavorite` field
      - On toggle: PATCH `/api/foods/:id` with `{ isFavorite: newValue }`
    - **Save button** to persist all editable fields via PATCH `/api/foods/:id`
    - Back link to `/app/foods`

    3. **Create recipe detail/edit page** at `src/routes/app/recipes/[id]/+page.svelte` (NEW FILE — does not exist):

    **Page server** (`+page.server.ts`):
    - Load recipe by ID (with ingredients) from the URL param
    - Verify the recipe belongs to the current user

    **Page component:**
    - Display a form pre-populated with the recipe's data (name, totalServings, ingredients list)
    - **Image upload field** (same pattern as food detail page):
      - Native file input, POST to `/api/images/upload`, update via PATCH `/api/recipes/:id`
      - Show preview or colored placeholder
    - **Favorite toggle:**
      - shadcn-svelte Switch or Checkbox labeled "Mark as favorite"
      - On toggle: PATCH `/api/recipes/:id` with `{ isFavorite: newValue }`
      - This works because we extended recipeUpdateSchema above to include isFavorite and imageUrl
    - **Save button** to persist changes via PATCH `/api/recipes/:id`
    - Back link to `/app/recipes`

    **i18n:** Add Paraglide message keys for detail page strings (food_detail_title, recipe_detail_title, mark_as_favorite, image_upload_label, upload_image_button, save_changes, back_to_foods, back_to_recipes) in `messages/en.json` and `messages/de.json`.
  </action>
  <verify>
    - `bun run check` passes
    - Navigate to `/app/foods/{some-food-id}` — food detail page renders with form, image upload, and favorite toggle
    - Navigate to `/app/recipes/{some-recipe-id}` — recipe detail page renders with form, image upload, and favorite toggle
    - Toggle isFavorite on a recipe — PATCH succeeds (not silently dropped)
    - Upload an image from either page — preview shows
  </verify>
  <done>Recipe validation schema includes isFavorite and imageUrl; food and recipe detail/edit pages exist with image upload and favorite toggle</done>
</task>

<task type="auto">
  <name>Task 3: AddFoodModal favorites tab upgrade</name>
  <files>
    src/lib/components/entries/AddFoodModal.svelte
  </files>
  <action>
    1. Update `src/lib/components/entries/AddFoodModal.svelte`:

    **Replace favorites tab content:**
    - The favorites tab currently shows a text list (`onlyFavorites` filter on foods array)
    - Replace with FavoritesGrid + FavoriteCard components
    - Import FavoriteCard and FavoritesGrid from `$lib/components/favorites/`
    - Filter foods with `onlyFavorites()` as before, but render as FavoriteCard grid
    - Each card's `onTap` should call `handleAddFood(food.id)` (existing function) — this uses the modal's servings input, not tap-to-log
    - Also show favorite recipes in the favorites tab (filter recipes that have isFavorite, render as FavoriteCards with `onTap` calling `handleAddRecipe`)
    - If no favorites, show a small empty state message

    **Note:** The AddFoodModal's favorites tab logs via the modal's own servings field (the user picks servings in the modal UI), NOT via the instant tap-to-log flow. The FavoriteCard's `onTap` here selects the item, and the existing modal flow handles servings + save.
  </action>
  <verify>
    - `bun run check` passes
    - Open AddFoodModal, switch to favorites tab — shows FavoriteCard grid instead of text list
    - Tapping a card in the modal selects the food/recipe for logging via the modal's flow
  </verify>
  <done>AddFoodModal favorites tab uses FavoriteCard grid for both foods and recipes instead of text list</done>
</task>

</tasks>

<verification>
- `bun run check` passes
- Dashboard widget shows top 5 favorites (combined ranking), respects preference toggle
- Tap-to-log works from dashboard widget with undo
- AddFoodModal favorites tab displays card grid
- Food detail page at /app/foods/:id exists with image upload and favorite toggle
- Recipe detail page at /app/recipes/:id exists with image upload and favorite toggle
- PATCH /api/recipes/:id accepts isFavorite and imageUrl fields
- Start page redirect goes to /app/favorites
- All existing tests still pass
</verification>

<success_criteria>
- FavoritesWidget renders on dashboard when enabled, shows combined top 5 with tap-to-log
- AddFoodModal favorites tab upgraded from text list to visual cards
- Food detail/edit page created with image upload and favorite toggle
- Recipe detail/edit page created with image upload and favorite toggle
- Recipe validation schema extended with isFavorite and imageUrl
- Start page redirect correctly points to /app/favorites
</success_criteria>

<output>
After completion, create `.planning/phases/02-favorites/02-03-SUMMARY.md`
</output>
