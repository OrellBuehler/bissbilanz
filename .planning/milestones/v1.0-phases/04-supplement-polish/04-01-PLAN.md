---
phase: 04-supplement-polish
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - src/lib/server/schema.ts
  - src/lib/server/validation/supplements.ts
  - src/lib/components/supplements/SupplementForm.svelte
  - src/routes/app/+page.svelte
  - messages/en.json
  - messages/de.json
autonomous: true
requirements:
  - SUPP-02
  - SUPP-04
  - SUPP-05

must_haves:
  truths:
    - "Dashboard supplement widget respects showSupplementsWidget preference toggle"
    - "Supplement form allows selecting a time of day (morning, noon, evening, or none)"
    - "timeOfDay column exists in supplements table and persists through API"
  artifacts:
    - path: "src/lib/server/schema.ts"
      provides: "timeOfDay column on supplements table"
      contains: "time_of_day"
    - path: "src/lib/server/validation/supplements.ts"
      provides: "timeOfDay field in create/update schemas"
      contains: "timeOfDay"
    - path: "src/lib/components/supplements/SupplementForm.svelte"
      provides: "Time of day selector in supplement form"
      contains: "timeOfDay"
    - path: "src/routes/app/+page.svelte"
      provides: "Widget gated on userPrefs.showSupplementsWidget"
      contains: "showSupplementsWidget"
  key_links:
    - from: "src/lib/components/supplements/SupplementForm.svelte"
      to: "src/lib/server/validation/supplements.ts"
      via: "form payload includes timeOfDay field"
      pattern: "timeOfDay"
    - from: "src/routes/app/+page.svelte"
      to: "userPrefs"
      via: "conditional rendering"
      pattern: "userPrefs\\?\\.showSupplementsWidget"
---

<objective>
Add timeOfDay column to supplements schema, update validation and form, and fix the dashboard widget to respect user preferences.

Purpose: Enables time-of-day scheduling (SUPP-02) at the data layer and fixes the widget visibility bug (SUPP-04).
Output: Migration for timeOfDay column, updated form with time-of-day selector, corrected dashboard widget gating.
</objective>

<execution_context>
@/home/orell/.claude/get-shit-done/workflows/execute-plan.md
@/home/orell/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/04-supplement-polish/04-RESEARCH.md
@src/lib/server/schema.ts
@src/lib/server/validation/supplements.ts
@src/lib/components/supplements/SupplementForm.svelte
@src/routes/app/+page.svelte
@messages/en.json
@messages/de.json
</context>

<tasks>

<task type="auto">
  <name>Task 1: Add timeOfDay column and update validation</name>
  <files>
    src/lib/server/schema.ts
    src/lib/server/validation/supplements.ts
    messages/en.json
    messages/de.json
  </files>
  <action>
1. In `src/lib/server/schema.ts`, add a `timeOfDay` column to the `supplements` table definition (after `sortOrder`):
   `timeOfDay: text('time_of_day')` — nullable, accepts 'morning', 'noon', 'evening', or null (meaning "anytime").

2. Run `bun run db:generate` to create the migration. Verify the generated SQL only adds the column (ALTER TABLE ADD COLUMN), no destructive changes.

3. In `src/lib/server/validation/supplements.ts`:
   - Add `timeOfDay: z.enum(['morning', 'noon', 'evening']).nullable().optional()` to `supplementCreateSchema` (inside the `.object()`, before the `.refine()`).
   - Add the same `timeOfDay` field to `supplementUpdateSchema`.

4. In `messages/en.json`, add these keys:
   - `"supplements_time_of_day": "Time of day"`
   - `"supplements_time_morning": "Morning"`
   - `"supplements_time_noon": "Noon"`
   - `"supplements_time_evening": "Evening"`
   - `"supplements_time_anytime": "Anytime"`

5. In `messages/de.json`, add the same keys with German translations:
   - `"supplements_time_of_day": "Tageszeit"`
   - `"supplements_time_morning": "Morgens"`
   - `"supplements_time_noon": "Mittags"`
   - `"supplements_time_evening": "Abends"`
   - `"supplements_time_anytime": "Jederzeit"`
  </action>
  <verify>
    `bun run db:generate` succeeds (migration file created).
    Start dev server briefly (`bun run dev`) to confirm migration applies without errors, then stop it.
    `bun run check` passes with no type errors.
  </verify>
  <done>
    timeOfDay column exists in supplements table. Validation schemas accept timeOfDay. i18n keys exist for all time-of-day labels.
  </done>
</task>

<task type="auto">
  <name>Task 2: Add time-of-day selector to form and fix dashboard widget gating</name>
  <files>
    src/lib/components/supplements/SupplementForm.svelte
    src/routes/app/+page.svelte
  </files>
  <action>
1. In `src/lib/components/supplements/SupplementForm.svelte`:
   - Add a `timeOfDay` state variable initialized from `supplement?.timeOfDay ?? null`.
   - Add a time-of-day selector using the existing `Select.Root` pattern (same as dosageUnit/scheduleType selectors). Options: Morning, Noon, Evening, Anytime (null). Use the new i18n keys (`m.supplements_time_morning()`, etc.). Label: `m.supplements_time_of_day()`.
   - Place the selector after the schedule section (after the schedule-related conditional blocks, before the button row).
   - Include `timeOfDay` in the `handleSubmit` payload. Send `null` when "Anytime" is selected.

2. In `src/routes/app/+page.svelte`:
   - Change line 239 from `{#if supplementChecklist.length > 0}` to `{#if userPrefs?.showSupplementsWidget}`.
   - This makes the supplement widget respect the settings toggle, matching the pattern used by favorites (`userPrefs?.showFavoritesWidget`) and weight (`userPrefs?.showWeightWidget`) widgets.
  </action>
  <verify>
    `bun run check` passes.
    Start dev server, navigate to /app/supplements, create/edit a supplement — time-of-day selector appears and saves correctly.
    Navigate to /app (dashboard) — supplement widget visibility is controlled by Settings > Supplements Widget toggle, not by checklist length.
  </verify>
  <done>
    Supplement form includes time-of-day selector with four options. Dashboard supplement widget is gated on `userPrefs?.showSupplementsWidget`. SUPP-02 data layer complete. SUPP-04 widget bug fixed.
  </done>
</task>

</tasks>

<verification>
- `bun run check` passes with no errors
- New migration file exists in `drizzle/` directory
- Dev server starts cleanly (migration applies)
- Supplement CRUD includes timeOfDay field end-to-end
- Dashboard widget respects showSupplementsWidget preference
</verification>

<success_criteria>
- timeOfDay column in supplements schema with migration
- Validation schemas accept timeOfDay
- SupplementForm has time-of-day selector
- Dashboard widget gated on userPrefs.showSupplementsWidget (not checklist length)
- i18n keys for time-of-day labels in en and de
</success_criteria>

<output>
After completion, create `.planning/phases/04-supplement-polish/04-01-SUMMARY.md`
</output>
