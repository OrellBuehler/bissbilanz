---
phase: 03-weight-tracking
plan: 03
type: execute
wave: 2
depends_on: [03-01]
files_modified:
  - src/lib/components/weight/WeightWidget.svelte
  - src/routes/app/+page.svelte
autonomous: true
requirements: [WGHT-06]

must_haves:
  truths:
    - "Dashboard shows latest weight when showWeightWidget is enabled"
    - "Dashboard hides weight widget when showWeightWidget is disabled"
    - "Widget displays weight in kg and the date it was logged"
    - "Widget links to /app/weight for full details"
  artifacts:
    - path: "src/lib/components/weight/WeightWidget.svelte"
      provides: "Dashboard widget showing latest weight"
      min_lines: 15
    - path: "src/routes/app/+page.svelte"
      provides: "Dashboard page with weight widget integrated"
      contains: "WeightWidget"
  key_links:
    - from: "src/routes/app/+page.svelte"
      to: "/api/weight/latest"
      via: "fetch on mount"
      pattern: "fetch.*api/weight/latest"
    - from: "src/routes/app/+page.svelte"
      to: "src/lib/components/weight/WeightWidget.svelte"
      via: "conditional render based on showWeightWidget"
      pattern: "showWeightWidget"
---

<objective>
Add the weight widget to the dashboard, gated by the existing showWeightWidget user preference.

Purpose: Gives users quick visibility of their latest weight on the dashboard without navigating to the weight page.
Output: WeightWidget component and dashboard integration.
</objective>

<execution_context>
@/home/orell/.claude/get-shit-done/workflows/execute-plan.md
@/home/orell/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/03-weight-tracking/03-RESEARCH.md
@.planning/phases/03-weight-tracking/03-01-SUMMARY.md
@src/routes/app/+page.svelte
@src/lib/components/supplements/SupplementChecklist.svelte
</context>

<tasks>

<task type="auto">
  <name>Task 1: Weight widget and dashboard integration</name>
  <files>
    src/lib/components/weight/WeightWidget.svelte
    src/routes/app/+page.svelte
  </files>
  <action>
    1. Create WeightWidget.svelte — small card component:
       - Props: weightKg (number), entryDate (string), optional link to /app/weight
       - Display: Weight icon, latest weight formatted to 1 decimal (e.g., "78.5 kg"), date of entry
       - Include a "View all" or arrow link to /app/weight
       - Use shadcn-svelte Card, matching the visual style of FavoritesWidget and SupplementChecklist
       - Handle null/empty state: show "No entries yet" with prompt to log first weight
       - Use i18n messages for all text (weight_latest, weight_no_entries, nav_weight)

    2. Integrate into dashboard (+page.svelte):
       - Add state: `latestWeight` (nullable object with weightKg and entryDate)
       - On mount (alongside existing data loading), fetch GET /api/weight/latest
       - If response ok, set latestWeight from response.entry
       - In template, conditionally render WeightWidget:
         `{#if userPrefs?.showWeightWidget}<WeightWidget weightKg={latestWeight?.weightKg} entryDate={latestWeight?.entryDate} />{/if}`
       - Place the widget in the dashboard layout near other widgets (after favorites, before or after supplements — follow existing widget order pattern)
       - The `showWeightWidget` preference already exists in the schema and settings page, so no additional wiring needed
  </action>
  <verify>
    `bun run check` passes. `bun run dev` and visit /app — weight widget appears when preference is enabled and latest weight exists.
  </verify>
  <done>
    Dashboard shows weight widget with latest entry when showWeightWidget is true. Widget hidden when preference is false. Widget displays weight in kg with date and links to /app/weight.
  </done>
</task>

</tasks>

<verification>
- `bun run check` passes
- Dashboard renders weight widget when enabled in settings
- Widget shows correct latest weight and date
- Widget hidden when disabled in settings
- Widget links to /app/weight
</verification>

<success_criteria>
Dashboard weight widget fully functional, gated by existing showWeightWidget preference. Shows latest weight with date, links to full weight page.
</success_criteria>

<output>
After completion, create `.planning/phases/03-weight-tracking/03-03-SUMMARY.md`
</output>
